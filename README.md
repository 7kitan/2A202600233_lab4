## LAB 4
- System prompt trong file: `prompts/system_prompts.xml`
- Add prompt to force calling `calculate_budget` tool when a budget is given by user
- Add prompt to ignore ambiguity in date when running tests (causes Tests 2 and 3 to fail and asks the user again for leaving date without calling any tool)
- Agent in `agent.py`
- Test cases in `tests/test.json`
- Test output for tests 1-5 in `logs/test_logs.txt`
